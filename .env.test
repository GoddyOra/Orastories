# Dummy placeholder values for the Vitest environment only - createClient()
# doesn't make a network call at construction time, so these just need to be
# non-empty strings to satisfy lib/supabaseClient.ts's guard. Safe to commit:
# not real credentials, and tests never actually call out to Supabase.
VITE_SUPABASE_URL=https://test-placeholder.supabase.co
VITE_SUPABASE_ANON_KEY=test-placeholder-anon-key
